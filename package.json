{
  "name": "getpostflow",
  "private": true,
  "version": "0.1.0",
  "license": "UNLICENSED",
  "packageManager": "pnpm@9.15.4",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --filter=@getpostflow/web",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "seed:demo-client": "dotenv -e apps/web/.env.local -- npx ts-node --project tsconfig.scripts.json scripts/seed-demo-client.ts",
    "seed:demo-clients": "dotenv -e apps/web/.env.local -- npx ts-node --project tsconfig.scripts.json scripts/seed-demo-clients.ts",
    "seed:demo-content": "dotenv -e apps/web/.env.local -- npx ts-node --project tsconfig.scripts.json scripts/seed-demo-content.ts",
    "seed:demo-inbox": "dotenv -e apps/web/.env.local -- npx ts-node --project tsconfig.scripts.json scripts/seed-demo-inbox.ts",
    "seed:demo-analytics": "dotenv -e apps/web/.env.local -- npx ts-node --project tsconfig.scripts.json scripts/seed-demo-analytics.ts",
    "grant-admin": "dotenv -e apps/web/.env.local -- npx ts-node --project tsconfig.scripts.json scripts/grant-admin.ts",
    "fix-org-subscription": "dotenv -e apps/web/.env.local -- npx ts-node --project tsconfig.scripts.json scripts/fix-org-subscription.ts"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.1.11",
    "@types/node": "^22.15.30",
    "@types/react": "^19.1.8",
    "@types/react-dom": "^19.1.6",
    "drizzle-kit": "^0.31.4",
    "pg": "^8.21.0",
    "tailwindcss": "^4.1.11",
    "ts-node": "^10.9.2",
    "tsx": "^4.19.4",
    "turbo": "^2.0.14",
    "typescript": "^5.8.3",
    "vitest": "^4.1.7"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "dotenv": "^17.4.2",
    "dotenv-cli": "^11.0.0",
    "drizzle-orm": "^0.44.7"
  }
}
